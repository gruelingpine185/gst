<%- include('../partials/head') %>
<section class="hero">
  <div class="container">
    <div class="row">
    <div class="seven columns">
      <h1 class="font-black text-multiline"><%= heroSection.title %></h1>
        <% if(heroSection.bullets.length) { %>
          <ul>
            <% for(let i = 0; i < heroSection.bullets.length; i++) { %>
              <li><%= heroSection.bullets[i] %></li>
            <% } %>
          </ul> 
        <% } %>
        <button class="button-primary"><p><%= heroSection.button.text %></p></button>
      </div>
      <div class="five columns">
        <% if(heroSection.image.path === '#') { %>
            <div class="placeholder-img"></div>
        <% } else { %> 
            <img src="<%= heroSection.image.path %>" alt="<%= heroSection.image.alt %>">
        <% } %>
      </div>
    </div>
  </div>
</section>
<%- include('../partials/footer') %>
